import LayoutTemplate from '../components/LayoutTemplate';
import JoinFormContainer from '../containers/JoinFormContainer';

const Join = () => {
  return (
    <LayoutTemplate name="join">
      <JoinFormContainer />
    </LayoutTemplate>
  );
};

export default Join;
